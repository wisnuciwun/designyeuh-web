import React, { Component } from 'react'

class SelectedCategory extends Component {
        // changePagePopular = (page) => {
    //     this.setState({
    //         offset: (page - 1),
    //         activePage: page
    //     }, () => this.paging())
    // }

    // componentDidMount = async () =>
    // {
    //     await this.orderBy()
    // }

    // render() {

    // let items = [];

    // if(this.state.allPage != 0)
    // {
    //     for (let i = 1; i <= this.state.timeSorted.allPage; i++) {
    //         items.push(
    //           <Pagination.Item style={{cursor: 'pointer'}} key={i} onClick={() => this.changePageTime(i)} active={this.state.timeSorted.activePage}>
    //             {i}
    //           </Pagination.Item>,
    //         );
    //       }
      
    // }
    
    render() {
        return (
            <div>
                selectet kategori
                
            </div>
        )
    }
}

export default SelectedCategory
